import HomeButton from "../components/HomeButton";

/**
 * ReadingHistoryPage
 * --------------
 * Allows user to manually add a book to reading history.
 */
export default function ReadingHistoryPage() {
  return (
    <div>
      <HomeButton />
      <h2>Add Book to Reading History</h2>
    </div>
  );
}