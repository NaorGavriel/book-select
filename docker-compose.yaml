services:
  api:
    build: ./server
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./server/app:/code/app
    ports:
      - "8000:8000"
